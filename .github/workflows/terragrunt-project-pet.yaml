name: Terragrunt PET

on: pull_request

permissions:
  pull-requests: write
  contents: write
  id-token: write

jobs:

  define-targets:
    name: Test Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Terragrunt HCL Check
        uses: apentsak-vitech/gh-checks/github/publish-checks@main
        with:
          name: Terragrunt HCL Check
          conclusion: false
          description-file: terraform1.txt

      - name: Terragrunt HCL Check
        uses: apentsak-vitech/gh-checks/github/publish-checks@main
        with:
          name: Terragrunt HCL Check
          conclusion: false
          description-file: terraform2.txt
